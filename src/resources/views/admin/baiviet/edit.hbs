{{> headerAdmin}}
{{> sidebar}}
<link rel="stylesheet" href="https://cdn.quilljs.com/1.3.6/quill.snow.css">
<main id="main" class="main">
    <div class="pagetitle">
        <h1>Chỉnh sửa dịch vụ</h1>
    </div><!-- End Page Title -->
    <section class="section dashboard">
        <div class="row">
            <div class="col-lg-12">
                <!-- Recent Activity -->
                <div class="card">
                    <div class="card-body">
                        <h5 class="card-title" id="currentYear"> </h5>
                        <div class="box-center">
                            <div class="add-event-form">
                                <input type="hidden" id="content" value="{{baiviet.content}}">
                                <br>
                                <form id="event-form">
                                    <div class="form-group">
                                        <label class="event-title">Tiêu đề:</label>
                                        <input type="text" class="form-control" value="{{baiviet.title}}" id="title"
                                            name="title" style="width:30%">
                                    </div>
                                    <div class="form-group">
                                        <label for="recipient-name" class="col-form-label">Chọn thể loại:</label><br>
                                        <select class="form-select" id="category" aria-label="Default select example">
                                            <option value="Giới thiệu">Giới thiệu</option>
                                            <option value="Dịch vụ">Dịch vụ</option>
                                            <option value="Tin tức">Tin tức</option>
                                        </select>
                                    </div>
                                    <!-- Trường nhập file ẩn -->
                                    <input type="file" id="image" style="display: none;" name="image" accept="image/*">
                                    <label for="fileInput">Bạn cần chọn lại hình ảnh:
                                        <!-- Ảnh sẽ được hiển thị và click vào đây sẽ mở trình duyệt file dialog -->
                                        <img id="imagePreview" src="/{{baiviet.image}}" alt="Preview">
                                    </label>
                                    <div class="form-group">
                                        <label for="message-text" class="col-form-label">Nội dung:</label>
                                        <div id="editor" style="min-height: 300px;"></div>
                                    </div>
                                    <br>
                                </form>
                                <button type="submit" class="btn btn-primary" onclick="onclickEdit()">Lưu Lại</button>
                            </div>
                        </div>
                        <div id="getcontent" style="display: none;" getid="{{baiviet._id}}"></div>
                    </div>
                </div><!-- End Recent Activity -->
                <!-- Budget Report -->
            </div><!-- End Right side columns -->
        </div>
    </section>
</main><!-- End #main -->

{{> footerAdmin}}
<script src="https://cdnjs.cloudflare.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
<script src="https://cdn.quilljs.com/1.3.6/quill.js"></script>
<script>

    var img = document.getElementById('image')
    var imagePreview = document.getElementById('imagePreview')
    //lấy nội dung
    const getcontent = document.getElementById('getcontent')
    const content = document.getElementById('content').value
    getcontent.innerHTML = content
    var contentWithHTML = document.getElementById('getcontent').innerHTML;
    //getid
    const getid = getcontent.getAttribute('getid')

    // quill text
    var quill = new Quill('#editor', {
        theme: 'snow',
        modules: {
            toolbar: [
                ['bold', 'italic', 'underline', 'strike'],  // Các công cụ in đậm, in nghiêng, gạch chân, gạch đầu dòng
                ['link', 'image'],  // Công cụ liên kết và hình ảnh
                [{ 'header': [1, 2, 3, 4, 5, 6, false] }],  // Công cụ chọn tiêu đề
                [{ 'color': [] }, { 'background': [] }],  // Công cụ chọn màu văn bản và nền
                ['list', 'bullet', 'indent', 'outdent'],  // Công cụ danh sách và thụt đầu dòng
                ['undo', 'redo'],  // Công cụ hoàn tác và làm lại
            ],

        }
    });
    //load data vẫn giữ  nguyên định dạng
    quill.clipboard.dangerouslyPasteHTML(contentWithHTML);

    // Khi click vào ảnh, chuyển sự kiện click đến trường nhập file
    imagePreview.addEventListener('click', function () {
        img.click();

    });
    // change ảnh khi chọn
    img.addEventListener('change', function () {
        if (img.files && img.files[0]) {
            const reader = new FileReader();
            reader.onload = function (e) {
                imagePreview.src = e.target.result;
            };
            reader.readAsDataURL(img.files[0]);
        }
    });

    //clickedit
    function onclickEdit() {
        var title = document.getElementById('title').value
        var category = document.getElementById('category').value
        const file = img.files[0]

        if (file && title && category) {
            const formData = new FormData();
            formData.append('image', file);
            formData.append('id', getid);
            formData.append('title', title);
            formData.append('category', category);
            formData.append('editor', quill.root.innerHTML);
            fetch(`/admin/bai-viet/${getid}`, {
                method: 'PUT',
                body: formData,
            })
                .then(() => {
                    window.location.replace('/admin/bai-viet')
                })
        }

    }
</script>